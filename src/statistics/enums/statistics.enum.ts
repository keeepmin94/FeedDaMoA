export enum StatisticsType {
  DATE = 'DATE',
  HOUR = 'HOUR',
}

export enum StatisticsValue {
  COUNT = 'COUNT',
  VIEW_COUNT = 'VIEW_COUNT',
  LIKE_COUNT = 'LIKE_COUNT',
  SHARE_COUNT = 'SHARE_COUNT',
}
